{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "cd backend && composer install --no-dev && php artisan migrate --force && php -S 0.0.0.0:$PORT -t public",
    "restartPolicyMaxRetries": 5
  }
}
